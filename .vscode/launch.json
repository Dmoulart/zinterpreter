{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "./zig-out/bin/interpreter",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
       
        }
    ]
}